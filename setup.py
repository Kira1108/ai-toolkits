from setuptools import setup
from setuptools import find_packages

setup(name='ai_toolkits',
      version='0.0.1',
      description='the fucking ai toolkits',
      author='The fastest man alive.',
      packages=find_packages(),
      install_requires=[])